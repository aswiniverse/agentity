apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentity.fullname" . }}
  labels:
    {{- include "agentity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentity.selectorLabels" . | nindent 4 }}
